import React from "react";

export const Admin = () => {
    // PrivateRout component replaced this logic
    // const {isLoggedIn} = useContext(AuthContext)
    // if (!isLoggedIn) {
    //     return <Redirect to="/"/>
    // }

    return (
        <div>
            <h1>Admin</h1>
        </div>
    )
}